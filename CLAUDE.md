# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Multi-tenant SaaS for UK dental practice groups. Two apps: a Fastify-style **Express backend** (`backend/`, deploy Railway) and a **Next.js 14 frontend** (`frontend/`, deploy Railway via `frontend/Dockerfile`, service `web`). Postgres + RLS on Supabase. Target launch Fri 30 May 2026.

## Commands

Backend (`cd backend`):
- `npm run dev` — watch-mode server on :8080 (`node --watch src/server.js`)
- `npm start` — prod server
- `npm test` — vitest (all). Single file: `npx vitest run path/to/x.test.js`. Single test: `npx vitest run -t "name"`
- `npm run test:integration` — `vitest.integration.config.js`
- `npm run lint` — eslint `src --ext .js`
- `npm run seed` — `../scripts/seed-tenant.js`. (Note: `npm run migrate` points at `../scripts/migrate.js` which does **not** exist — migrations are managed by Supabase, see below. Don't rely on `npm run migrate`.)

Frontend (`cd frontend`):
- `npm run dev` — :3000 · `npm run build` · `npm run lint` (next lint) · `npm run typecheck` (`tsc --noEmit`)

Local DB: the live Supabase project is rooted at repo-root `supabase/` (`config.toml` + `supabase/migrations/`). Run `supabase start` then `supabase db reset` **from the repo root** — it applies all `supabase/migrations/20260101*_*.sql` in numeric order. The current ledger runs `000001`→`000048`: schema, RLS, seed, `000004_access_token_hook` (creates the Custom Access Token Hook — critical, see rule 8), then RBAC/`role_permissions`, user status, business-health cadence, integrations (GHL/Dentally/Xero/CSV), `platform_admins`, the `auth_bootstrap` + rollup/payment-summary/settled-receipts RPCs, signup-approval, then the post-`000020` Dentally/analytics layer — `pms_patient_id`/`pms_practitioner_id` on appointments, growth practice-performance + chair-utilisation + treatment-mix/production RPCs, associate/staff Dentally mapping, `dentally_treatment_plans`, health manual metrics, GHL upsert indexes, and `lead_ghl_stage`. Then the LMS layer — `000045` courses catalogue, `000047` `course_modules` + `lesson_files` tables + `module_id` backfill on existing lessons, `000048` `course_resources.category` (course-level material folders: marking-rubrics | additional-resources). All are idempotent and re-apply cleanly. The `db/01_schema.sql`/`02_rls.sql`/`03_seed.sql` files are unmanaged source copies, **not** what `supabase db reset` reads; keep them in sync with `supabase/migrations/` when changing schema. After any hosted DDL run `NOTIFY pgrst, 'reload schema';` (PostgREST cache goes stale — recurring gotcha).

Backend `npm run typecheck` is `find src -name '*.js' -exec node --check {} +` (syntax check, no TS); `npm run build` is a no-op echo. `.github/workflows/ci.yml`: backend job runs typecheck/lint/**test**/build; frontend job runs typecheck/lint/build only (**no `npm test` step** — frontend tests are not gated by CI). All scripts exist; CI is green.

## Backend architecture

Strict layering, one domain per file across parallel dirs:

```
routes/ -> controllers/ -> services/ -> repositories/ -> models/
```

- **routes/** — `*.routes.js`, each exports an `express.Router`; wired in `src/app.js`.
- **controllers/** — parse/validate with the domain's Zod schema from `models/`, call the service, shape the HTTP response. No business logic.
- **services/** — business logic, orchestration, formula calls.
- **repositories/** — only Supabase data access ("queries in, rows out"). No logic.
- **models/** — Zod schemas (`*CreateSchema`, `*ListQuerySchema`, etc.), not ORM models.
- `src/app.js` `buildApp()` is the composition root; `src/server.js` is the entry point.

`backend/` is **native ESM** (`package.json` `"type": "module"`; `import`/`export`, relative imports carry `.js` extensions). It was converted from the old compiled-CommonJS output — there is still no `.ts` source or `tsconfig`. Write idiomatic ESM JS; do **not** reintroduce `require`/`module.exports`/`__importDefault`. Convention in converted files: namespace imports keep their original local var (`import * as x_1 from "../y.js"`); named `export const`/`export function`; route files `export default router`.

### Request flow & multi-tenancy (read before touching data access)

`app.js` mounts public routes (`/healthcheck`, `/webhooks`, `/auth`) without auth, then everything under `/api` behind `authenticate` then `audit`.

`middleware/auth.js`:
- Verifies the Supabase JWT via `serviceClient.auth.getUser`.
- Loads the `users` row, sets `req.user = {id, email, organisation_id, role, access_token}`.
- Attaches `req.db = tenantClient(token)` (RLS-respecting client).
- Exports `requireRole(...roles)` route gate — use for RBAC on protected endpoints.

`app.js` also serves a public HTML status page at `/` (Supabase connectivity check on boot). CORS allowlist is hardcoded (`dev.elevate.app`, `staging.elevate.app`, a Railway URL) plus `process.env.FRONTEND_URL`.

`lib/supabase.js` exposes two clients:
- `serviceClient` — **bypasses RLS**. Intended for webhooks/workers only.
- `tenantClient(token)` — RLS-scoped per request (`req.db`).

**Reality vs. intent:** repositories currently use `serviceClient` and enforce tenant isolation by manually chaining `.eq('organisation_id', orgId)` on every query, passing `req.user.organisation_id` down through controller → service → repo. RLS via `req.db` is available but not the path repos take. When adding repo methods, you MUST replicate the explicit `organisation_id` filter — there is no automatic isolation on the service-client path. Mutations are logged by `audit` middleware to `audit_log`.

Stripe webhook needs the raw body: `app.js` mounts `express.raw` on `/webhooks/stripe` **before** the global JSON parser — keep that ordering.

`lib/`: `claude.js` (Anthropic, model `claude-sonnet-4-5-20250929`), `formulas.js`, `supabase.js`, `postmark.js`, `twilio.js`, `messaging.js` (email/SMS dispatch), `crypto.js` + `webhook-token.js` + `oauth-state.js` (integration secret encryption / webhook + OAuth-state signing), `integrations/` (per-provider connectors — Dentally, GHL, Xero, CSV), `permissions.js` (RBAC catalog), `platform-admin-bootstrap.js`, `snapshot-utils.js`. `workers/index.js` runs persistent `node-cron` jobs (e.g. monthly business-health snapshots, nightly GHL sync) using `serviceClient`; run as a separate process. `workers/ghl-sync-once.js` is a one-shot variant: it runs `syncAllOrgs()` once then `process.exit` — for a schedule-managed Railway "Cron Schedule" service (Start Command `node src/workers/ghl-sync-once.js`, schedule `0 22 * * *` UTC). Do NOT run both index.js and ghl-sync-once for the same job.

**Sentry**: `src/instrument.js` (`@sentry/node`) MUST be imported before any other module (`server.js` imports it first); it is a no-op unless `SENTRY_DSN` is set, so it is safe to always load. `sendDefaultPii: true` — keep Sentry data scrubbing on for patient/financial fields. Structured logging via `pino`/`pino-http`. **Stale artifact:** `backend/dist/` is leftover compiled-CommonJS output from before the ESM conversion — ignore it; the live source is `backend/src/`.

### Financial code

All money is **integer pence** — never floats. `lib/formulas.js` is the single source for financial calcs (`calculatePL`, `calculateValuation`, `calculateAssociatePay`, `calculateCashFlow`, `calculateKPIs`, `calculateLTV`, `calculateMarketingROI`, `calculateProgress`, `calculateCAGR`, helpers `pence`/`pct`/`formatPounds`). Any new/changed formula must update `docs/FORMULAS.md` and add a unit test (accountant reviews `FORMULAS.md` before launch).

## Frontend architecture

Next.js 14 App Router. Route groups: `app/(auth)` (login/signup/forgot), `app/(dashboard)` (~60 tenant product pages), `app/(platform)` (superadmin console — orgs, signup queue, change-password). Feature-first modules under `frontend/features/` (contacts, crm, dashboard, finance, growth, health, integrations, intelligence, leads, operations, overview, payments, practices, settings, system, training, wealth, `_mock`); shared UI primitives in `frontend/components/ui` (+ `components/{dashboard,layout,setup}`). `lib/format.ts` holds money/display helpers.

**Server-side cookie auth (do not regress):** JWT lives in an **httpOnly cookie**, never in client JS. Two same-origin proxies, one per auth system: `app/api/backend/[...path]/route.ts` injects the tenant Supabase Bearer token; `app/api/platform-backend/[...path]/route.ts` injects the `platform_token` for the superadmin console. Both forward server-side to the Express backend. `middleware.ts` uses `@supabase/ssr` cookie sessions and gates `/platform` on the platform token; `lib/supabase-browser.ts` / `lib/supabase-server.ts` are the client/SSR Supabase helpers. The two JWT systems stay isolated — the single `/login` page tries tenant `/auth/login`, falls back to platform login on a plain 401.

Current code is root-level (`frontend/{app,components,features,lib}`); a move under `frontend/src/` is documented in `TODOS.md` but **deferred** (blocked on Railway deploy stability) — don't start it unprompted.

React Query for server state, Tailwind + `class-variance-authority` for UI, `recharts` for charts. `preview/elevate-dental-os-v2.html` is the visual reference prototype for porting pages.

## Project rules (from README "Key principles" — do not violate)

1. No dark mode — light/white only.
2. Money in £ pence integers; display `(pence/100).toLocaleString('en-GB')`.
3. Tenant isolation: every business table has `organisation_id`; only webhooks/workers may use `serviceClient` to bypass RLS conceptually.
4. British English in all UI (organisation, colour, optimise, centre).
5. Practice Manager finance access is Owner-toggled; Reception role = CRM only (Inbox, Pipeline, Contacts) — nothing else.
6. "Italy Implant Residency" is not a live offering — exclude everywhere.
7. No emojis in code/UI except explicitly specified spots.
8. Supabase Custom Access Token Hook is critical — without it RLS returns zero rows and the app silently appears broken.
9. Audit every mutation to `audit_log` (user_id, org_id, diff).

## Branch / deploy model

`develop` → staging.elevate.app (auto), `main` → app.elevate.app (auto). Solo developer owns the repo — push straight to `main`; keep CI green and update `docs/API.md` for any new endpoint. Deploy scripts in `scripts/`. Deeper detail in `docs/`: `ARCHITECTURE.md`, `API.md`, `FORMULAS.md`, `DEPLOYMENT.md`, `TESTING.md`, `DAILY_TASKS.md`.

## Current state (working session)

Done and committed on `main` (now in sync with `origin/main` — pushed). Note: this section is a point-in-time work log; the durable architecture lives in the sections above, which are authoritative when they conflict with this log.
- **Frontend UI**: all ~50 dashboard screens ported pixel-faithful from `preview/elevate-dental-os-v2.html`, mock-data only (`features/<section>/`, `features/_mock`), shared `components/ui` primitives. Login two-column port.
- **Dynamic RBAC**: `role_permissions` table (migration `…000005`) + code catalog `backend/src/lib/permissions.js`. Precedence: `catalog-deny < CODE DEFAULT_ROLE_PERMISSIONS < DB role_permissions (admin override) < users.permissions`. Backend never locks an owner out on DB/cache failure (code defaults are the safety net). Admin Team Permissions UI wired to live API. RLS stays the org-isolation hard boundary; perms are app-layer.
- **Auth hardening**: `/auth/login` gated on an active `public.users` row; orphan-safe signup/invite (reclaims dangling `auth.users`); `removeMember` deletes both `public.users` and `auth.users`; `users.status` (`invited`|`active`, migration `…000006`); invite → set password → active flow.
- **Backend → native ESM** (whole `backend/src`, 107 files). Tests: vitest, 47 passing (`npm test` = `vitest run`), incl. cross-org isolation.
- **Perf**: middleware no longer does a blocking backend `/auth/me` per navigation; one shared cached `useMe()` (`frontend/hooks/useMe.ts`) replaces 3 uncached fetches in sidebar/topbar/team screen.
- **Signup approval flow** (migration `…000011`, applied on hosted): `users.status` widened to `invited|active|pending|rejected`. Two owner-creation paths, both via `provisionOrgOwner` in `auth.service.js`: (1) public `POST /auth/signup` → owner `pending`, login hard-blocked (403) until a platform **superadmin** approves; (2) `POST /api/platform/orgs` (superadmin) → owner `active` now + one-time temp password returned once. Platform queue: `GET /api/platform/signups`, `POST .../signups/:id/{approve,reject}` (reject keeps the row as `rejected`). Frontend: `(platform)/platform/signups` + create-owner form on `…/orgs`. Tenant owners still self-serve more owners/reception via `provisionMember`. See memory `signup-approval-flow`.
- **Unified login** (frontend only; the two JWT systems stay isolated): the single `/login` page → Next route `app/auth/login/route.ts` tries tenant `/auth/login` first, falls back to `POST /api/platform/login` on a plain 401, sets the matching cookie (Supabase session vs `platform_token`), and redirects (`/dashboard` vs `/platform/overview`). The standalone `/platform/login` page + `/api/platform-auth/login` route + `platformLogin()` were removed. `middleware.ts`: `/platform` w/o token → `/login`; admin on `/login` → `/platform/overview`. Login route forwards the real client IP (`x-forwarded-for`) so per-IP rate-limiters key per client, not per proxy.
- **Auth security fixes**: RBAC matrix routes (`/api/admin/permissions/*`) now `requireRole('owner')` (was the delegable `permissions.manage` key — a self-escalation / owner-lockout hole; the catalog key is now effectively owner-only). `/auth/login` + `/auth/signup` get a dedicated 5/min/IP limiter; global limiter no longer keys on the spoofable `x-user-id` header. Platform `must_change_password` enforced server-side (403 on every route but `/me` + `/change-password`) with a matching UI: `(platform)/platform/change-password` + `MustChangePasswordGuard`. See memory `rbac-perms-override-ceiling`.
- **Auth hot path**: `authenticate` now resolves the user + role_permissions in ONE DB round trip via the `auth_bootstrap` RPC (migration `…000010`, applied on hosted), falling back to the old 2-query path if the RPC is absent.
- **Tests**: vitest, ~224 `it/test` calls across 25 files in `backend/test` (was 47, then 122). Cover signup→pending, login approval gate, platform create/approve/reject, `auth_bootstrap` fast-path + fallback, the `must_change_password` gate, and cross-org isolation. Test harness has `.rpc` support (`test/setup.js`).
- **Platform superadmin** seeded in `platform_admins` (hosted): `ruhith@plan4growth.com` (`must_change_password=false`). Log in via the main `/login` page.
- **Integrations** (migrations `000013`–`000020`): Dentally (practice mapping + webhook panel + sync overlay), GoHighLevel, Xero, and CSV import landed — per-provider connectors in `backend/src/lib/integrations/`, routes `integrations.routes.js` / `oauth.routes.js` / `csv-import.routes.js`, secrets encrypted (`crypto.js`). Payments dashboard backed by exact summary + rollup RPCs (`000017`–`000020`) scoped to practice + date range. File uploads use S3 (`@aws-sdk/client-s3`, `files.routes.js`).
- **GoHighLevel multi-subaccount** (migration `000085_integration_accounts`, branch `feat/ghl-multi-subaccount`): one org can connect N GHL subaccounts (one per GHL Location), each via its own Private Integration Token, mapped 1:1 to a practice. New `integration_accounts` table (per-location encrypted PIT + `practice_id` + random `webhook_token` + per-account `config`/status) — the single `integrations` GHL row stays a lightweight "connected" marker. New `integration-account.repository.js` (org-scoped, secrets-safe `SAFE_COLS`), `ghl-account.service.js` (add/list/update/remove, validates the PIT via `GET /locations/{id}` before persisting), and 7 owner-only routes under `/api/integrations/gohighlevel/accounts`. `gohighlevel-sync.js` gained `syncAccount`/`bootstrapAccount`/`detectPipelinesForToken` and threads `practice_id` through every contact/lead write (the `syncOneOrg` legacy path is unchanged, only optional trailing params added); the worker `syncAllOrgs` now fans out over `integration_accounts`. Webhooks route by the per-account random `webhook_token` (`getByWebhookToken`) with a legacy signed-org-token fallback, stamping the account's `practice_id`. Frontend: `GoHighLevelPanel` is now a subaccount manager (add/map-to-practice/sync/disconnect + per-account webhook URL); Contacts/Leads/Pipeline filter by the global practice scope (`useScopePeriod`, UUID-guarded → `practice_id`). Migration backfills the existing GHL connection into one account row + stamps existing GHL contacts/leads when the org has exactly one practice. **Migration NOT yet applied on hosted** (a parallel `000084_communications` migration is in flight on `main`; coordinate before applying). **Fast-follow**: retire the legacy GHL `syncOneOrg`/broker-connect path so there's a single account-scoped entry, and ungate the subaccount panel for fresh orgs (today it shows only once the legacy `integrations` row is active).
- **GoHighLevel consolidated dashboard** (branch `feat/ghl-dashboard`, migration `000087_ghl_dashboard_rpc`): a dedicated GHL dashboard under Elevate CRM at `/ghl-dashboard` that sums contacts/leads/pipeline-value/conversions/conversations/sync-health across ALL connected subaccounts, with a per-subaccount filter + the shared ScopePeriod window. Backed by ONE live-aggregate Postgres RPC `ghl_dashboard_aggregate(p_org, p_since, p_until, p_practice)` — groups `contacts`/`leads`/`communications` by `practice_id` in SQL (communications has no `practice_id`, so practice is derived via a LEFT JOIN to `contacts.contact_id`; won=`treatment_started`/`treatment_completed`, lost=`not_proceeding`/`failed_to_attend`, open=the rest). New `ghl-dashboard.repository.js` (RPC wrapper), `ghl-dashboard.service.js` (totals + `perAccount[]` incl. an `accountId:null` "Unmapped" bucket for null-practice rows), endpoint `GET /api/integrations/gohighlevel/dashboard` (`requireRole('owner','practice_manager')`, static route before `/:provider/*`). Frontend slice `frontend/features/ghl/` (api/hooks + components); GHL summary cards with click-to-breakdown on `GroupOverviewScreen` (windowed via ScopePeriod). **Migration `000087` + `000085_integration_accounts` ARE applied on hosted** (`NOTIFY pgrst` run). Money is integer pence end-to-end. Spec/plan in `docs/superpowers/`. **GHL calendars + appointments** (migration `000088_ghl_appointments`, applied on hosted): per-subaccount calendar bookings synced into a dedicated `ghl_appointments` table (kept separate from clinical `appointments`) via a defensive 3rd phase in `syncAccount` (`fetchCalendars` + `pullAppointments`, GHL `/calendars/` + `/calendars/events`; missing calendars scope degrades gracefully). Aggregate RPC `ghl_appointments_aggregate`; dashboard shows an Appointments card (total + upcoming), a status + by-calendar panel, and an appointments column in the breakdown table. 38 GHL backend tests pass.
- **AI context snapshot** (Phase 1, migration `000066` applied on hosted): period-keyed JSONB cache `ai_context_snapshots` (one row per org+`period_key` = `YYYY-MM`|`YYYY`; `is_final` freezes closed months). `services/ai-context.service.js` owns `buildSnapshot`/`getSnapshot` (lazy recompute on 6h TTL, closed months never rebuilt) + `invalidatePeriods`/`finalizePreviousMonth`; `analyticsService.getLiveContextData` now delegates to the cached snapshot (drop-in, same shape + `meta`/`trailing12`). **Empty-data guard**: `task.service`/`p4g-ai.service` short-circuit Gemini and return a "connect your data — missing: …" reply (with a `missing[]` field) when `meta.data_coverage` shows no financials/baseline/appointments — fixes data-less orgs (e.g. Plan4Growth) looking broken. **Injection defense**: free-text labels sanitized via `lib/ai/sanitize.js` (`sanitizeForContext` strips control chars + neutralizes `</business_data>`), bundle wrapped in `buildContextString` delimiter; appointment/lead/patient notes excluded from the snapshot entirely. Nightly worker cron warms current month (`30 2 * * *`) + finalizes prev month day 3 (`0 3 3 * *`). Spec/plan in `docs/superpowers/`. **Deferred**: Task 8 sync-invalidation hooks (collides with in-flight integration WIP); Phase C conversation history.
- **AI drill-down tools (Phase B)**: native Gemini function-calling added to `lib/ai/providers/gemini.js`; provider-agnostic `lib/ai/tool-loop.js` runner (executes tool calls up to a round cap, runs a final schema-formatting turn for JSON surfaces, sums usage across turns); single `get_metrics` tool (`lib/ai/tools/get-metrics.js`) — period-keyed (cached via Phase 1 `getSnapshot`) OR arbitrary date-range (live windowed `assembleLiveContext`) + per-practice scope; `orgId` bound server-side, never a tool param (tenant isolation). Wired into all 5 AI surfaces (chat coach, analyst, board report, insights, task generation); JSON surfaces preserve their output contracts via the final schema turn. `assembleLiveContext` widened to accept `{period,since,until,scope}` object alongside the legacy string arg (back-compat). No migration, no frontend change.
- **Emergent practice mapping + real-time webhook (SHIPPED — code written by Gemini, reviewed + tested + committed by Claude; migration `000100` applied on hosted)**: two additions to the existing Emergent (Treatments Accepted) integration — (1) an explicit Emergent-business → practice mapping UI on the Integrations page (replaces the fuzzy `business_name` substring match in `emergent-sync.js` as the primary resolver; fuzzy stays as fallback), and (2) wiring the currently-stubbed `/webhooks/emergent/:token` handler to actually ingest real-time events. Webhook contract (from the client): `POST` with `X-Webhook-Event: treatment.{accepted|updated|deleted}` + `X-Webhook-Signature: sha256=<hmac-hex>` over the raw body, body `{ event, fired_at, data: {<pull row shape>} }` — byte-identical scheme to the live Dentally webhook (raw-body mount + HMAC-SHA256 vs per-org `config.webhook_secret`). Design: new `emergent_practice_map` table (`organisation_id`+`business_id` unique → `practice_id`, null=intentionally unmapped) + a real `business_id` column on `treatment_accepted`; auto-discovery of businesses on every sync/webhook; instant `restampPractice` backfill on a mapping change (no re-sync); `accepted`/`updated`→upsert, `deleted`→delete by derived `external_id`. Reuses the existing generic `/:provider/webhook-secret` route + `mergeConfig` by adding `'emergent'` to `WEBHOOK_PROVIDERS`. **Record identity — FIXED in migration `000149`** (this note previously described the bug as an accepted limitation; it was causing real double-counting). Emergent still sends no stable record id, but identity is now a DB-enforced natural key: `UNIQUE NULLS NOT DISTINCT (organisation_id, source, business_id, accepted_date, patient_norm, treatment_norm)` over normalised generated columns. Cosmetic edits (whitespace/case) and re-pricing now UPDATE the record instead of forking it — `amount` is deliberately NOT part of the identity. Before the fix, Plan4growth held 975 rows for 745 real records and £1,014,647 of overstated accepted value (228 of 229 duplicate pairs differed by nothing but a trailing space). 230 rows were archived to `treatment_accepted_dedup_archive`, reversible. **Residual limitation:** a genuine correction to business/date/patient-name/treatment-name is a new identity, so it still inserts and orphans the old row — inherent without a stable upstream id. The upsert's conflict target is the natural key, so a row whose `external_id` predates the fix heals itself on the next sync. Spec: `docs/superpowers/specs/2026-06-16-emergent-practice-mapping-and-webhook-design.md`; 14-task TDD plan: `docs/superpowers/plans/2026-06-16-emergent-practice-mapping-and-webhook.md`. **Status: DONE.** Code committed (Gemini wrote Tasks 1-13 across commits `2f0d087`→`bf2ae00`; Claude review fixes in `bf2ae00`: webhook now uses the same explicit+fuzzy `loadResolution` as the nightly sync rather than explicit-only, added the cross-org isolation test, fixed a stale comment). 22 emergent backend tests pass; frontend typecheck/lint/build clean. **Migration `000100` IS applied on hosted** (verified: `emergent_practice_map` created — 4 businesses discovered, 2 auto-mapped from existing fuzzy links; `treatment_accepted.business_id` backfilled 491/491; `NOTIFY pgrst` run). Backend env reminder: the live webhook URL renders only if `OAUTH_STATE_SECRET` + `BACKEND_PUBLIC_URL`/`APP_URL` are set, and real-time deliveries are rejected (401) until the owner sets the webhook signing secret in the Emergent panel.

- **P&L account-level drill-down (SHIPPED)**: the `/financial` "P&L & Margin" group statement (`features/intelligence/components/PLMarginScreen.tsx`) now expands each summary line into the individual accounts behind it — clicking Revenue / Lab & materials / Staff & clinician pay / Other operating costs reveals every QuickBooks (or Xero/manual) account line under it (e.g. Other operating costs → Council Tax, Advertising/Promotional, Utilities…), largest-first, matching the QBO P&L report exactly. The detail was always captured — every `monthly_financials` row carries `account_code` (the accounting feed's account name) — but the read path collapsed it to the 6-line bucket summary and dropped the per-account detail. New pure helper `accountLinesByPeriod()` in `monthlyFinancial.service.js` keeps `Map<period, Map<bucket, Map<account_code, pence>>>` applying the SAME synced-overrides-manual precedence as `bucketsByPeriod` (manual account lines dropped when a synced row exists for that period+bucket). `analytics.service.plMargin` accumulates a group-level `breakdown` (`{revPence,labMaterialsPence,staffPence,otherOpexPence}` → `{name,amountPence}[]`, sorted desc) over the SAME keys + SAME periods that built the statement total, so detail reconciles to each line. `monthlyFinancial.repository.allForOrg` widened its select to include `account_code`. Frontend: `PLBreakdown`/`PLBreakdownLine` types + EMPTY default in `pl-margin-api.ts`; expandable rows (▸/▾ + count badge, click-to-toggle) in `PLMarginScreen.tsx`. **No migration, no re-sync** — data already in `monthly_financials`; works per-company + accrual/cash via the existing QuickBooks filters. 2 new tests (QBO report shape + precedence); `pl-margin.test.mjs` 11/11 pass; frontend typecheck/lint clean. **Status: DONE, on `main`.**

- **Call Reporting — Google Sheets lead-response dashboard (SHIPPED, commit `41bca3b`; migration `000118` applied on hosted + verified)**: `/call-reporting` page (8 stat cards: total/called<3m/called<10m/efficiency%/in-pipeline/not-called/Facebook/Google leads, practice + single-date filters) fed by a tenant-connected Google Sheet of raw lead rows. Connect = Google OAuth with ONLY `spreadsheets.readonly` (no Drive scope — owner pastes the sheet URL; `parseSpreadsheetId`), one `google_sheets` provider row (tokens AES-GCM via `crypto.js`), one `sheet_sources` row per org + one-time five-column mapping (practice / created / first-call / source / pipeline-status — ONLY these five columns are ever read or stored; PII columns never leave Google). Practice resolution = explicit `sheet_practice_map` (Emergent pattern, never name-match) with instant `restamp_sheet_lead_practices` RPC on change. Rows land in `sheet_leads` (unique `source_id+sheet_row_index`, sha256 `row_hash` diff); freshness = on-view append-only top-up (60s in-memory debounce, ranged batchGet from `last_synced_row+1`) + nightly 03:40 full re-sync (worker `google-sheets-sync`, catches in-place edits, retries failed sources) + manual refresh. Cards = ONE RPC `sheet_leads_dashboard(p_org,p_date,p_practice,p_tz)` (London date bucketing; fb/google via source regex). Timestamps: serial numbers converted in the sheet's own timezone (DST-correct via Intl), dd/mm/yyyy text fallback. Routes: static `/api/integrations/google-sheets/*` (owner; status/practice-map also PM) + `GET /api/call-reporting/dashboard` (owner+PM). Frontend: `features/call-reporting/` + `GoogleSheetsPanel` (4-state setup wizard on Integrations), nav under Elevate CRM gated `growth.view` (NOT `crm.view` — Reception must not see it, rule 5). 40 new backend tests (parse/repo/service incl. cross-org isolation + no-token-leak); suite 1476 green. Env needed before OAuth works: `GOOGLE_SHEETS_CLIENT_ID/SECRET` (falls back to the Google Ads pair) + Sheets API enabled on the Google Cloud project. Spec: `docs/superpowers/specs/2026-08-04-call-reporting-google-sheets-design.md`. **v2 — multi-sheet, 10 cards (SHIPPED, migration `000119` applied on hosted + verified)**: reworked to one Google Sheet PER PRACTICE instead of one org-wide sheet. `sheet_sources` gained `practice_label` — a self-contained free-text field the owner types on connect (e.g. "Bexleyheath"), deliberately NOT a foreign key to `practices` (mirrors how the source sheets themselves are practice-specific, no join needed); `sheet_practice_map` table + `restamp_sheet_lead_practices` RPC DROPPED entirely (no mapping step, no restamp). The five mapped columns changed to Date / Created Time / Called-3m / Called-10m / Pipeline Name — `sheet_leads` gained `called_3m`/`called_10m` (booleans, Yes/No bucket parsing) + `pipeline_name` (text), and the old `first_call_at`/practice/source columns are gone; date parsing prefers Sheets serial numbers with an MM/DD/YYYY (US-style, not the v1 dd/mm/yyyy) text fallback. Dashboard grew from 8 to 10 cards: added Office Time / Outside Office Time, bucketed Mon–Fri 09:00–17:00 Europe/London against `created_at`; Facebook/Google now regex-match `pipeline_name` instead of a source column. New RPC `sheet_leads_dashboard` (same name, new signature/columns: `called_3m`, `called_10m`, `office_time`, `outside_office` alongside totals/in_pipeline/not_called/facebook/google). Routes moved to per-source: `/api/integrations/google-sheets/sources/:id/*` (was one org-wide source) so an owner manages N sheets, one per practice. Migration `20260101000119_call_reporting_multi_sheet.sql` **IS applied on hosted project `mkfhpzjbijbachoonytt`** (verified: `sheet_leads` has `called_3m`/`called_10m`/`pipeline_name`, no `first_call_at`; `sheet_sources.practice_label` present; `sheet_practice_map` gone; `sheet_leads_dashboard` RPC present; `NOTIFY pgrst` run) — the migration WIPES all v1 `sheet_leads` rows (schema-incompatible), so every practice's sheet must be reconnected by the owner post-deploy. Backend suite 1500 green, lint 0 errors, typecheck clean, frontend build clean. **Deploy note**: backend + frontend must ship together (breaking API/route shape change).

- **GHL→Dentally conversion export to Google Sheets (SHIPPED — merged to `main` + pushed, migration `000121` APPLIED on hosted)**: when a patient's **first-ever** Dentally appointment lands (webhook or nightly sync), and they also exist as a GoHighLevel contact with a lead in a pipeline (matched by normalised email or UK phone, exact equality — email first, phone fallback), one row is appended to an owner-connected Google Sheet recording the conversion (name, email, phone, GHL pipeline name, appointment date, lead-incoming date). One spreadsheet, one tab auto-created per practice, plus a trailing Export ID column for idempotent dedup on re-append. Architecture is an outbox queue + drainer (Approach B, spec-decided with the owner): both ingest paths (Dentally webhook, nightly sync) do one cheap idempotent thing in the hot path — insert a `sheet_export_queue` row (`ON CONFLICT DO NOTHING`) — all Google I/O happens only in the drainer, kicked fire-and-forget after the webhook 200 (60s in-process debounce/org) plus a `*/15` cron sweep (`workers/index.js` job `sheet-export-drain`) that retries and catches sync-path inserts. No pipeline match → row marked `no_match`, revisited for 30 days; destination sheet gone 403/404 → whole integration flips to `failed` with a specific reason (Integrations panel shows "reconnect") rather than retrying forever. **Separate connection from Call Reporting**: new provider `google_sheets_writer` holds the FULL `https://www.googleapis.com/auth/spreadsheets` scope (read/write) — deliberately isolated from the read-only `google_sheets` (Call Reporting) provider row, which is untouched. Reuses `GOOGLE_SHEETS_CLIENT_ID/SECRET` env (falls back to the Google Ads pair) and the already-registered OAuth redirect URI, so no new callback route — but **the `spreadsheets` scope must be added to the Google Cloud OAuth consent screen for that client before an owner can connect** (prerequisite, not yet done). **Go-forward-only**: `config.export_since` is stamped ONCE, at the moment the owner sets the destination sheet (`POST .../destination`), to "now" — no backfill of pre-existing appointments; outage windows during the go-forward period are not lost (the enqueue-since-`export_since` step on every drain catches anything queued but not yet drained). New files: migration `20260101000121_sheet_export_queue.sql` (`sheet_export_queue` table + enqueue/claim(FOR UPDATE SKIP LOCKED)/phone-candidate RPCs), `lib/integrations/google-sheets-writer-provider.js` (OAuth provider), `lib/integrations/google-sheets-writer.js` (tab-per-practice writer, Export ID dedup), `services/sheet-export-match.service.js` (email/phone matcher against GHL contacts+leads), `services/sheet-export.service.js` (drain orchestration), `repositories/sheet-export.repository.js`, `controllers/sheet-export.controller.js`, 4 routes under `/api/integrations/google-sheets-writer/*` (status: owner+practice_manager; destination/drain/disconnect: owner), Integrations panel UI (connect/destination/status/export-now). ~50 new backend tests across 6 `sheet-export-*.test.mjs` files. **Migration `000121` IS applied on hosted project `mkfhpzjbijbachoonytt`** (verified: table + 3 RPCs + 4 indexes present, RLS on, anon/authenticated EXECUTE revoked, service_role granted; `NOTIFY pgrst` included in the migration). **Deploy note**: backend + frontend ship together but the change is additive/non-breaking (new provider, new routes, no existing shape changes) — no forced-together constraint like the Call Reporting v2 migration above. Spec: `docs/superpowers/specs/2026-08-06-ghl-dentally-sheet-export-design.md`.

- **Data Room — analyst-ready (Phase 1 of the ETL/rehearsal-org spec, SHIPPED on `feat/data-room`)**: derived-column views `public.data_room_*` + summary RPCs `data_room_practice_day/month` (migration `000131`, applied on hosted, `NOTIFY pgrst` run) give the analyst the dashboard's rules in the rows (`occurred`/`dna`/`patient_key`/`practitioner_name`/`outcome`…) and a new `summaries` source (practice by day / month, reconciles to `appointments_rollup_by_practice` + settled receipts — check with `scripts/data-room-reconcile.sql`; verified against Ashford May 2026, where the RPC's `appointments` column — patient appointments of any status, distinct from `occurred` which is completed-only — reconciles to 801). Registry: 24 datasets across 6 sources (`dentally`, `google-ads`, `meta-ads`, `gohighlevel`, `emergent`, `summaries`). Excel export (`export.xlsx`, `exceljs`, one worksheet per practice, `_gbp` neighbour columns, 500k row cap), `GET /api/data-room/freshness` badge, column dictionary (`lib/data-room/dictionary.js` → drawer in the UI + generated `docs/DATA_ROOM_DICTIONARY.md` via `npm run data-room:dictionary`; `validateRegistry()` fails on any undocumented column). Spec: `docs/superpowers/specs/2026-08-27-etl-pipeline-rehearsal-org-and-analyst-data-room-design.md`; Phases 2–3 (etl_runs ledger, runner, rehearsal org) not started.

- **Agency model Phases A1+A2 (SHIPPED 2026-08-31, on `main`, migration `000133` applied on hosted)**: Elevate is now an agency→sub-accounts SaaS. **A1** — `organisations.parent_organisation_id`+`is_agency` + `org_features` overrides over the code catalog (`lib/features.js`: internal keys `data_room`/`emergent`/`call_reporting`/`sheet_export` default OFF and seeded ON for the 3 existing orgs, module keys default ON), `requireFeature` route gates on the four internal families + runtime `PROVIDER_FEATURE` gate on the generic `/api/integrations/:provider/*` entry points (revoke deliberately ungated), workers skip disabled orgs, `/auth/me.features`, FE nav/FeatureGate hiding. **A2** — `/api/agency/*` (sub-account list/create via `provisionOrgOwner` w/ one-time temp password, per-child feature toggles, switch); org switching = user-bound 12h HMAC token (`lib/agency-switch.js`) in an httpOnly `agency_switch` cookie set by Next route `/api/agency-switch`, re-injected as `x-agency-switch` by the backend proxy, re-validated per request in `authenticate` (cached `org-meta.service`; invalid → silent home context); switched mutations audit `diff.via_agency`; `/auth/me.agency` shape; mapping mutations (ad-attribution ×3, `practices/:id/pms-site-id`, `emergent/practices`, GHL `practice_id` field) are `requireAgencyActor`-gated with reads open; FE topbar switcher + switched banner + `/agency` screen + mapping controls hidden for non-agency-actors. Spec `docs/superpowers/specs/2026-08-31-saas-feature-gating-and-isolation-design.md`; plans in `docs/superpowers/plans/`. Backend 1833 tests green. **Remaining**: A3 module-key enforcement on route mounts, A4 isolation audit (`docs/ISOLATION_AUDIT.md`), Sub-project B tenant lead fetch direct from Google Ads (`lead_form_submission_data`) + Meta (`leads_retrieval` — App Review is the long pole).

- **Agency Phases A3 + A4 (SHIPPED 2026-08-31, on `main`, no migration)**: **A3** — module keys enforced at the route mount via `requireFeature`, but ONLY on mounts consumed by exactly one nav section (`crm`: contacts/comms/workflows/crm-templates/crm-settings; `operations`: appointments/chair-utilisation/associates/staff/pay-runs; `finance`: payments/monthly-financials; `training`; `system`: imports). Anything an always-on **Overview** page also reads (`/analytics`, `/growth`, `/wealth`, `/health`, `/leads`, `/treatments`, `/tasks`, `/integrations`, `/practices`, `/finance/quickbooks`, `/ad-attribution`) stays UNGATED — Overview has no module key by design. `business_health`/`growth`/`wealth` own no exclusive mount → nav-only toggles. `/admin/team` + `/admin/permissions` deliberately ungated (a system-off org must keep its own team admin). `test/features.module-gates.test.mjs` pins both the gated and must-stay-ungated lists. Agency actors BYPASS module gates (they administer the sub-account) but never internal-feature gates. Frontend mirrors via `SECTION_FEATURE` in `lib/permissions.ts`. **A4** — full isolation audit, `docs/ISOLATION_AUDIT.md`. Systemic finding: the org filters were nearly all present; the holes came from **PostgREST embeds** (`contact:contacts(...)`) resolving FKs as joins under `serviceClient` with no org predicate, so any body-supplied FK written unchecked became a cross-org PII read. Fixed via new `lib/tenant-guard.js` (`assertOrgOwns` + `stripImmutable`) applied across 9 services; plus a **critical cross-org write** (`contactUpdateSchema`/`appointmentUpdateSchema` were `z.record(z.any())`, making `organisation_id` writable — any user could PATCH their row into another tenant); plus `/api/admin/logs` restricted to agency actors (process-wide pino files carry every tenant's data). Backend 1886 tests green. Test-infra fix: `vitest.config.js` gained `testTimeout: 30s` — several suites legitimately run 2.5-3.5s and randomly tripped the 5s default under parallel workers, failing a different test most runs.

- **Agency access model CORRECTED (migration `000134`, applied on hosted)**: agency powers are a **per-user grant** (`users.is_agency_admin`), NOT "owner of an org with `is_agency`". The 000133 blanket seed had given Plan4growth's four client users sub-account creation, practice mapping and production logs. `organisations.is_agency` now only marks the ONE org that owns sub-accounts (**Plan4growth**); an agency admin may sit in a different org and still administer it, so the acting org is resolved server-side (`orgMetaService.getAgencyOrgId()` → `req.agencyOrgId`) and the switch check validates the target against that, never the caller's own org. Superadmin grants via `PATCH /api/platform/users/:id/agency-admin`. Frontend `isAgencyActor` fails closed.
- **CallRail multi-company call tracking (SHIPPED, on `feat/callrail-integration`; migrations `000154`+`000155` both applied on hosted, verified)**: ONE new table, `callrail_calls` — credentials reuse the existing `integration_accounts` table, one row per CallRail company (no second table, no singleton key-paste route: the first company added IS the connection, mirroring GoHighLevel multi-subaccount). A call's practice comes from the company that fetched it, denormalised onto every row; deliberately no tracking-number map, and an unmapped company attributes its calls to nothing. Calls stay OUT of `leads` — no pipeline/opportunity shape, and folding them in would make cross-source GHL dedup (phone only; CallRail carries no email) an invisible write-time decision instead of an explicit read-time one. Two ingestion paths share one idempotency key, `UNIQUE (organisation_id, callrail_id)`: the nightly/manual pull and the webhook, which resolves its org from the path token ALONE, never the payload, then re-fetches the canonical call by id rather than trust the webhook's own differently-shaped (legacy numeric vs. API string) id. CallRail DOES sign webhooks — header `Signature`, HMAC-**SHA1**, per-company key — verified as an optional second factor behind the token.

- **Ad-platform reporting at deep grain (SHIPPED on `feat/ads-deep-grain`, migration `20260101000148_ad_deep_grain.sql` — APPLIED on hosted)**: campaign-grain ad reporting now has a second, finer layer underneath it — Google ad group / ad / keyword and Meta ad set / ad — landed as **five separate tables** (`ad_meta_adsets`, `ad_meta_ads`, `ad_google_adgroups`, `ad_google_ads`, `ad_google_keywords`), deliberately not one table with a `level` column, so no query can accidentally sum across grains. **Migration `000148` IS applied on hosted project `mkfhpzjbijbachoonytt`** (verified: five tables present with RLS enabled and zero policies, `parent_id` NOT NULL and `conversions` numeric on all five, 5 indexes each, and `anon`/`authenticated` EXECUTE false with `service_role` true on all six functions — `_ad_grain_table`, `_ad_grain_provider`, `ad_grain_replace_window`, `ad_grain_restamp_practices`, `ad_grain_rollup`, `ad_keyword_rollup`; `NOTIFY pgrst` run). The tables are EMPTY until the first nightly sync after deploy. Each table holds a rolling **92-day window**: the nightly replace deletes the account's existing rows outright and reinserts the fresh pull, so the tables always hold exactly that window, never an accumulating history. Reads are **RPC-only** (`ad_grain_rollup`, `ad_keyword_rollup`) — direct table selects are avoided because PostgREST silently truncates at 1000 rows (the same trap as `monthly_financials`). `parent_id` is `NOT NULL` and part of the unique key on every grain table because a Google ad (and a Google keyword) can appear under more than one ad group — Google's hierarchy has ads and keywords as **siblings under an ad group**, not keyword-under-ad. `conversions` is `numeric(14,2)`, not integer, because Google reports modelled/fractional conversions. New reconciliation endpoint `GET /api/marketing/reconciliation` (+ `AdReconciliationPanel` on the Integrations page) compares deep-grain spend back to the campaign-grain total per platform; the Google keyword shortfall (Dynamic Search Ads and Display/Video traffic carry no keyword) is expected and surfaced as an explained gap, not flagged as broken. Non-GBP ad accounts are refused rather than silently converted — `partitionAccountsByCurrency` in `ad-currency.js` keeps unsupported-currency accounts out of the deep pull entirely and reports them, on the view that a visible gap is recoverable but a wrong total that looks right is not. Deep pull runs after the existing campaign-grain replace on both nightly syncs and is wrapped so a throttled/failed keyword or ad-set pull can never fail the campaign sync. **Two corrections the final review forced, both worth remembering:** (1) routing a read through an aggregate RPC is NOT protection against PostgREST's silent 1000-row cap — `ad_grain_rollup` collapses day rows into ENTITY rows and the entity count is itself what exceeds 1000, so both rollup reads are PAGED (`.order('entity_id').order('parent_id')` + `.range()`, stopping on an EMPTY page never a short one; `entity_id` alone is not unique). Unpaged, a ~5,000-keyword org would have summed £35k against £176k and rendered a fabricated 80% gap as an *expected* shortfall. (2) An exclusion in the reconciliation is only correct if it PARTITIONS THE DATA: `is_selected` does not (neither sync consults it), so filtering the campaign side by it made the deep side legitimately larger and produced a false red mismatch. Backend 225 files / 2163 tests green, lint 0 errors (12 pre-existing warnings), typecheck clean; frontend typecheck/lint clean, build clean except the pre-existing `/forgot-password` prerender failure (no Supabase env at build time — documented, not a regression). **UI so far is the reconciliation panel ONLY** — the Facebook and Google reporting pages are sub-projects 2 and 4 and do not exist yet.

- **Facebook Reporting page (SHIPPED on `feat/facebook-reporting`, migration `20260101000149_ad_meta_funnel.sql` APPLIED on hosted)**: two new routes, `/marketing-facebook` (campaign tier) and `/marketing-facebook/[campaignId]` (ad set tier, with in-place ad expansion — one expansion level, no third route), plus a "Facebook" nav entry under Marketing; `ROUTE_PERMISSION` gates `marketing-facebook` on `marketing.view`, so Reception stays out per rule 5. The migration widens `ad_lead_conversions` with `ad_id` appended LAST (byte-for-byte copy of its `000146` body plus that one column, verified by diff) and adds a new `ad_meta_funnel(p_org, p_since, p_until, p_practice)` RPC that reads THROUGH `ad_lead_conversions` rather than re-deriving booked/attended/converted, so every grain of this report shares the funnel's one definition of those terms. A lead is identified as Meta **structurally** — its `ad_id` resolves to a campaign id present in that org's own Meta `ad_metrics` rows (`ad_meta_funnel`'s `WHERE` clause) — never by a CRM label such as `attribution_source`: another tenant's CRM may name that channel differently, or not be GoHighLevel at all, and keying off a label would render an empty report that looks perfectly healthy instead of an honest zero-coverage state. The ad set is resolved by id only — `ad_id` → `ad_meta_ads.entity_id` → `parent_id` — never by name, because `contacts.ad_set_id` is null on every row GoHighLevel has ever sent this org; a `utm_medium`-matches-ad-set-name fallback was deliberately NOT built, because that match is unverified until the first sync actually populates `ad_meta_ads`. Four tenant states (`not_connected`, `never_synced`, `no_ad_id_coverage`, `ok`), computed per tenant from that tenant's own rows and displayed, not assumed. The window is clamped to the deep-grain tables' rolling 92-day floor (`clampWindow` in `facebook-report.service.js`, the same `DEEP_WINDOW_DAYS` floor `google-ads-deep-sync.js` uses) with `effectiveSince`/`windowClamped` returned and surfaced in the UI rather than clamped silently. Deliberate omissions, all labelled rather than hidden: no platform-reported-conversions column at ad set or ad grain (the funnel is the one conversions source), reach is ad-set-only and non-additive (never summed into a campaign total), "attended" is Dentally-only, and every cost figure (`cplPence`/`cpbPence`/`cpaPence`) is `null` — not zero — on a zero denominator. Backend 226 files / 2227 tests green, lint 0 errors (12 pre-existing warnings), typecheck clean; frontend typecheck/lint clean, build clean except the pre-existing `/forgot-password` prerender failure; `ggshield secret scan commit-range origin/main..HEAD` clean. **The final whole-branch review returned DO NOT MERGE on two Criticals, both fixed and both worth remembering.** (1) The lead window was EXCLUSIVE of `until` (`ad_meta_funnel`'s `l.created_at < $3`) while every spend window was INCLUSIVE (`.lte('metric_date', until)` and `metric_date <= $3`), and the service handed the same inclusive date to all three. Measured on live data: an August view returned 1,295 leads against a true 1,336 — the last day of every month silently lost — and a single-day selection returned ZERO leads beside that day's real spend, rendering `state: 'ok'` with no caveat. Fixed with a `funnelUntil()` conversion applied at the call site only, leaving the two DATE readers inclusive; a test asserts in ONE invocation that the funnel got `until + 1` while the other two got `until`. (2) `campaignSpendByProvider` had no practice parameter while the funnel and the ad-set rollups both honoured one, so a five-practice group filtering to one practice divided GROUP-wide spend by ONE practice's leads (~5x CPL) and the two tiers of the same report contradicted each other. Note the asymmetry that remains and is documented in code: `ad_metrics`/deep-table `practice_id` is stamped from the AD ACCOUNT's mapping, while the funnel's is the LEAD's own routing — so a practice-scoped row can legitimately show spend with no leads. Also removed: the ad-set Reach column, because `ad_grain_rollup` does not return `reach` and an always-blank column is worse than none — the data IS in `ad_meta_adsets`, so restoring it needs a new RPC and migration (ticketed, not done). **Task 8 also carried two fixes**: the shared test-window constant in `facebook-report.service.test.mjs` now derives `since` from `DEEP_WINDOW_DAYS` (`londonDaysAgo(DEEP_WINDOW_DAYS - 10)`) instead of a hardcoded date, so it can never drift below the moving 92-day floor and silently collapse ~20 tests onto the clamped-only path again; and `FacebookQuerySchema` gained a cross-field refinement rejecting `since > until` (equal `since`/`until`, a single-day selection, is explicitly accepted) — an inverted range previously reached `campaignSpendByProvider` unfiltered, matched nothing, and reported `never_synced` to a fully synced tenant, which is exactly the client bug this plan found and fixed elsewhere. **Migration `000149` IS applied on hosted project `mkfhpzjbijbachoonytt`** (verified after applying: `ad_id` is the LAST returned column of `ad_lead_conversions` so positional consumers are unaffected; `ad_campaign_funnel` still returns rows after the required DROP/CREATE, so the existing Marketing pages are unharmed; `ad_meta_funnel` returns 259 rows with 0 non-Meta; `anon`/`authenticated` EXECUTE false and `service_role` true on both functions; `NOTIFY pgrst` run).

### Next TODOs
- **Migrations on hosted Supabase**: all through `…000033` are now applied on project `Dental Os` (`mkfhpzjbijbachoonytt`) — the `000021`–`000033` Dentally/analytics layer (chair utilisation, treatment-mix/production RPCs, associate/staff mapping, treatment plans, health manual metrics, GHL indexes) landed in later sessions. The original schema (`000001-000003`, `000005-000006`, `000009`) was set up directly without the migration ledger; this session backfilled the gaps via the Supabase MCP — `000004_access_token_hook`, `000007_business_health_cadence`, `000008_integrations` (only the `integrations` table pre-existed), plus `000010_auth_bootstrap_rpc` and `000011_signup_approval`. All idempotent; they re-apply cleanly on the next local `supabase db reset`. **`000047` (`course_modules` + `lesson_files` + `module_id` backfill) IS applied on hosted** (verified: backfill nested all existing lessons under a default module, 0 orphans; `NOTIFY pgrst, 'reload schema';` run). **`000048` (`course_resources.category`) IS applied on hosted** (verified: column `text NOT NULL DEFAULT 'additional-resources'` + `course_resources_category_chk` constraint present; `NOTIFY pgrst, 'reload schema';` run). Reminder: after any hosted DDL run `NOTIFY pgrst, 'reload schema';` (PostgREST cache goes stale — recurring gotcha). **`000100_emergent_practice_map` (emergent_practice_map table + `treatment_accepted.business_id` column/backfill/seed) IS applied on hosted** (verified: table created, 491/491 treatment_accepted rows backfilled with business_id, map seeded with 4 businesses/2 auto-mapped; `NOTIFY pgrst` run). Re-applies cleanly on the next local `supabase db reset`.
- **Clear pre-existing orphan auth users**: Supabase → Authentication → Users (rows with no matching `public.users`). Future removes via Team UI won't orphan.
- **Branch model**: solo developer — push straight to `main` (in sync with `origin/main`). Open feature branches: `feat/gohighlevel-integration`, `fix/auth-middleware-405`, `fix/signup-backend-url-500`.
- Backend wiring: ~50 screens still mock — replace `features/*/data.ts`/`mock.ts` with real API per domain (separate slices).
- Frontend has no test framework; recharts not code-split (bundle); `frontend/src/` move still deferred (`TODOS.md`).
- **Enable the Custom Access Token Hook on hosted (rule 8)**: the `public.custom_access_token_hook` function now EXISTS on hosted (000004 applied this session) + is granted to `supabase_auth_admin`, but it still must be turned ON in Supabase → Authentication → Hooks → Custom Access Token (or via the Management API) — that toggle is GoTrue config, not SQL, so the MCP can't set it. Low impact today (repos use `serviceClient` + manual org filters, not the RLS/`req.db` path), but required before anything relies on `tenantClient`/RLS or on `organisation_id`/`role` JWT claims.

## Skill routing

When the user's request matches an available skill, invoke it via the Skill tool. When in doubt, invoke the skill.

Key routing rules:
- Product ideas/brainstorming → invoke /office-hours
- Strategy/scope → invoke /plan-ceo-review
- Architecture → invoke /plan-eng-review
- Design system/plan review → invoke /design-consultation or /plan-design-review
- Full review pipeline → invoke /autoplan
- Bugs/errors → invoke /investigate
- QA/testing site behavior → invoke /qa or /qa-only
- Code review/diff check → invoke /review
- Visual polish → invoke /design-review
- Ship/deploy/PR → invoke /ship or /land-and-deploy
- Save progress → invoke /context-save
- Resume context → invoke /context-restore
