# Handoff — end of day 2026-08-27

Read this first in the next session. Everything below is on `main` (`113b000`, pushed; Railway deploy started 21:50 IST).

## Where we are

**Goal of the week:** the data analyst starts **Monday 1 September 2026** and must be productive in the Data Room from day one. Phase 1 (below) is done; Phases 2–3 are designed, not started.

**Spec:** `docs/superpowers/specs/2026-08-27-etl-pipeline-rehearsal-org-and-analyst-data-room-design.md`
**Phase 1 plan (executed):** `docs/superpowers/plans/2026-08-27-phase1-analyst-data-room.md`

Decisions made with the owner (do not re-litigate): no raw landing layer / no second copy of data; no external warehouse; Data Room is the analyst's space (SQL login deferred); rehearsal organisation re-pulls **12 months for every source, payments included**, through the same integrations; **nothing existing is deleted or altered** before the owner reviews a comparison; minimisation = keep name/email/phone, drop DOB→birth_year, address, postcode→district, notes, `pms_patient` JSON, message bodies (Inbox shows who/when only).

## Done today

1. **Phase 1 — analyst-ready Data Room (shipped)**
   - Migration `000131`: 8 `public.data_room_*` views with derived rule columns (`is_patient_appointment/occurred/dna/cancelled/duration_mins/practitioner_name`, `patient_key/birth_year/postcode_district`, `is_settled`, `fee_total_pence`, `counts_as_activity`, `contact_key`, `pipeline_name/outcome`, `practice_name/cpl_pence`) + RPCs `data_room_practice_day/month`. Applied on hosted, verified: Ashford May 2026 **patient appointments = 801** (any status), occurred 528, DNA 54, cancelled 219. Financial columns are NULL per practice; group P&L sits on the `'Group / unassigned'` row. Month cost column is `cost_per_lead_pence` (spend ÷ CRM leads) — distinct from ads `cpl_pence`.
   - New `summaries` source (Practice by day / Practice by month) — registry now 24 datasets / 6 sources.
   - Excel export `GET /api/data-room/:source/:dataset/export.xlsx` (exceljs, one worksheet per practice + Unassigned, `_gbp` neighbour columns, 500k cap → 413, audited once even on abort).
   - Column dictionary (`backend/src/lib/data-room/dictionary.js`), drawer in the UI, generated `docs/DATA_ROOM_DICTIONARY.md` (`cd backend && npm run data-room:dictionary`); `validateRegistry()` fails on any undocumented column.
   - `GET /api/data-room/freshness` + "Data as of" badge; `/data-summaries` page first in the Data Room nav; analyst role lands there.
   - Reconcile check: `scripts/data-room-reconcile.sql` (cross-checks the summary RPC against `appointments_rollup_by_practice` / `settled_revenue_by_practice`).
   - Gates: backend 1,746 tests, lint 0 errors; frontend typecheck/lint/build clean. `docs/API.md` + `CLAUDE.md` updated.

2. **Production login outage found and fixed** (every login failed since 26 Aug 11:14)
   - Root cause: the Custom Access Token Hook runs as `supabase_auth_admin`; migration `000130` turned RLS on for `users`, whose policies call `current_org_id()`/`current_user_role()`, and `000129` had revoked EXECUTE on those → hook errored → GoTrue refused sign-in → app showed 401 "Invalid email or password".
   - Fix: migration `000132_auth_hook_rls_grants` (grants + `users_auth_admin_read` policy) — **already applied on hosted**, logins work again.
   - Hardening shipped with the deploy: backend `authClient` uses the service key (`28f41b6`); Next `/auth/login` sets the cookie via `setSession` from backend tokens instead of a second anon-key sign-in (`22dd954`). Login no longer depends on the captcha setting.
   - Memory note: `supabase-captcha-anon-login-401` (how to diagnose in seconds: throwaway user, not bogus creds).

3. Owner's dev account password was reset today (value given in chat — not stored here).

## Open items (tomorrow)

- [ ] **Confirm the Railway deploy succeeded** (`railway deployment list` — project superb-inspiration; Backend + Frontend). Then smoke-test production login.
- [ ] **Turn captcha protection OFF** in Supabase → project Dental Os (`mkfhpzjbijbachoonytt`) → Authentication → Attack Protection → Save. The earlier toggle did **not** take effect at the API. Until then *Forgot password* (browser → `/auth/v1/recover`) returns 400 `captcha_failed`. Verify: `cd backend && node --env-file=.env -e "import('@supabase/supabase-js').then(async({createClient})=>{const a=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_ANON_KEY,{auth:{persistSession:false}});console.log((await a.auth.signInWithPassword({email:'nobody@example.invalid',password:'x'})).error?.message)})"` → should say `Invalid login credentials`, not `captcha protection…`.
- [ ] **Click-through** (never done by a human yet): Data Room → Summaries (pills Practice by day / month; Ashford May 2026 row = 801; "Group / unassigned" row carries QuickBooks revenue/costs) → Dictionary drawer (Escape/backdrop close) → Export Excel (open in Excel: one tab per practice, `_gbp` columns, bold frozen header) → Dentally → Appointments (derived columns marked ` ·`).
- [ ] **Invite the analyst** before Monday: Team → invite → role *Analyst* → they should see only the six Data Room pages, landing on Summaries.
- [ ] Deferred minors from the reviews (none block anything): month RPC `nulls last` is dead (Group row sorts alphabetically); `dv()` carries `pii: undefined` for a test; drawer has no focus management; `sheetName` ignores Excel's reserved `History`; no "All practices" tab when scope=all (per-practice + Unassigned only); count queries on the appointments view are slower over multi-year windows (lateral join).

## Next big piece — Phases 2–3 (not started)

Write the plan with `superpowers:writing-plans` from the spec's Phase 2/3 sections, then execute with `superpowers:subagent-driven-development`:

- **Phase 2 — pipeline framework:** `etl_runs` ledger (migration `000133` — `000132` is now taken), `etl_run_id` on the 19 synced tables, run context in `backend/src/lib/etl/run.js`, per-org `organisations.minimise_pii` flag driving the minimised mapper (birth_year / postcode_district / email+phone hashes; no DOB, address, notes, `pms_patient`, message bodies), runner CLI `npm run etl -- --org <uuid> --source <x> --months 12 [--resume] [--sweep] [--dry-run]`, advisory-lock overlap guard, nightly cron skips an org with a running re-pull.
- **Phase 3 — rehearsal org:** platform console creates the org; `scripts/etl-clone-org-integrations.js --from <old> --to <new> --minimise` copies practices, integrations (tokens decrypt — global key), subaccounts (new webhook tokens), every practice mapping; **never** the Sheets writer / reports / notifications / workflows; 12-month re-pull; `scripts/etl-compare.js --old --new` per-practice-per-month report with the golden numbers (Ashford May 2026 patient appointments 801; treatment activity 421 / £79,757.72; plan fees collected ≈ 96%).
- Phase 4 (cutover) is a separate decision after the owner reads the comparison.

## Facts you will need

- Supabase project: `mkfhpzjbijbachoonytt` ("Dental Os"). The `supabase` CLI is **not installed**; apply migrations with the Supabase MCP `apply_migration`, then `notify pgrst, 'reload schema'`. Read auth logs with `query_logs` (`source = 'auth_logs'`; timestamps are UTC — local clock is IST, +5:30).
- Dev servers: backend `cd backend && npm run dev` (:8080), frontend `cd frontend && npm run dev` (:3000; stop it before `npm run build` — shared `.next`). Dev hits **hosted** Supabase. Backend `.env` has the rotated `sb_secret_`/`sb_publishable_` keys.
- Verification gates: `cd backend && npm test && npm run lint && npm run typecheck`; `cd frontend && npm run typecheck && npm run lint && npm run build`.
- Solo-dev branch model: work on a feature branch, merge to `main`, push → Railway auto-deploys.
