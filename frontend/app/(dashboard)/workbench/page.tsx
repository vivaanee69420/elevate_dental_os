import { TreatmentWorkbench } from '@/features/finance/components/TreatmentWorkbench';

// /workbench — the Intelligence OS Treatment Economics Workbench, its own
// Finance tab (split out of /profit).
export default function WorkbenchPage() {
  return <TreatmentWorkbench />;
}
